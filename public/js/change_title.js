function changePageTitle(title) {
    // Defina o novo título aqui
    document.title = title;
}

// Adiciona um ouvinte de evento que é acionado quando a página é totalmente carregada
/* document.addEventListener("DOMContentLoaded", function () {
    // Chama a função para alterar o título da página quando a página é carregada
    changePageTitle();
}); */
